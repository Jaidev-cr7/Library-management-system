name: library_app_mod
description: A modified library manager with modern nav and cover upload support.
publish_to: "none"
version: 0.0.1
environment:
  sdk: ">=2.18.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  firebase_core: ^4.0.0
  cloud_firestore: ^6.0.0
  firebase_storage: ^13.0.0
  firebase_data_connect: ^0.2.0
  provider: ^6.0.0
  google_fonts: ^6.3.0
  file_picker: ^10.2.4
  rxdart: ^0.28.0

flutter:
  uses-material-design: true
  assets:
    - assets/images/
